# Smart Launcher 6.6 build 021 拼音搜索增强版

文件：`Smart-Launcher-6.6-021-cleaned-pinyin-search.apk`

## 新增搜索方式

桌面搜索与应用网格页面搜索均支持：

- 拼音首字母：`微信` 可用 `wx` 搜索。
- 全拼：`微信` 可用 `weixin` 搜索。
- 未输入完整的连续全拼：`w`、`we`、`wei`、`weix`、`weixi` 都可找到 `微信`。
- 从后一个汉字开始：`xin` 也可找到 `微信`。
- 大小写和空格不敏感；多音字保留多个常见读音。

原有中文、英文及其他搜索规则仍由 Smart Launcher 自带匹配器处理；新增拼音结果只作为补充。

## 安全与兼容性

- 基于已移除已知注入代码的清理版制作。
- 未新增 Android 权限，`AndroidManifest.xml` 与清理版逐字节相同。
- 新增代码不联网、不动态加载代码、不创建后台线程。
- 只修改桌面应用搜索和应用网格搜索各一个调用点，其他原始方法保持不变。
- 使用与之前清理版、拼音首字母版相同的本地证书签名，可直接覆盖它们安装；不能覆盖官方版或其他签名版本。

## 校验

- SHA-256：`20296dca705e6c1a0f9a15732a589fc7a2f33e26f3dc8becdcac6eb844722eee`
- 79 项匹配断言通过。
- 80,000 次并发匹配压力测试通过。
- ZIP、DEX 校验和、V2/V3 签名和 16 KiB 对齐检查通过。

此处完成的是静态构建与验证，尚未在实际手机上启动测试。建议安装前保留 Smart Launcher 布局备份。

